### 🎯 模板标准命名

顶部树状图倒三角相关性热图 / 顶置聚类金字塔热图 (Inverted Triangular Correlation Heatmap with Top Dendrogram / Top-clustered Pyramid Heatmap)

### 🏷️ AI 检索高频关键词 (Tags)

建议将这些中英双语的高频专业术语作为标签保存，精准覆盖该图表的几何变换、空间排版与聚类特征：

- **基础图形** : 倒三角热图 (Inverted triangular heatmap), 金字塔热图 (Pyramid heatmap), 聚类热图 (Clustered heatmap), 相关性矩阵图 (Correlation matrix plot).
- **布局与拓扑** : 顶部树状图 (Top dendrogram / Top-aligned tree), -45度旋转 (-45 degree rotation), 半角矩阵保留 (Half-matrix retained / Triangular masking), 垂直空间对齐 (Vertical spatial alignment).
- **视觉细节** : 分类着色分支 (Color-coded branches / Branch highlight), 无坐标轴边框 (Hidden axes / No color borders), 右侧倾斜文本标签 (Slanted right labels).
- **适用场景** : 多组学相关性分析 (Multi-omics correlation), 微生物群落共生矩阵展示, 特征共线性空间压缩可视化.

### 📝 核心特征描述 (Prompt 提示词模板)

你可以直接复制这段简短的描述作为该模板的 AI 提示词（Prompt）。这段描述精准剥离了具体数据，提取了复刻该图表所需的所有拓扑关系和几何渲染逻辑：

> “这是一个**带顶部层次聚类树的倒三角（金字塔）相关性热图**。图表的核心主体是一个通过 -45度旋转 并隐藏一半冗余数据生成的倒三角形热图矩阵。其最大的排版特征在于：**上方横向放置了一个带有彩色分类高亮分支的层次聚类树状图（Dendrogram）**。树状图的末端节点向下延伸，与底部的倒三角热图矩阵实现了严格的几何居中与对齐。图表去除了多余的坐标轴线条，仅在热图右侧保留了倾斜的变量文本标签，整体呈现出高度压缩且对称的视觉高级感。”