### 🎯 模板标准命名

桑基图与堆叠柱状图组合图 / 流向与组分占比联动图 (Sankey-Stacked Bar Composite Plot / Flow and Composition Linked Chart)

### 🏷️ AI 检索高频关键词 (Tags)

建议将这些中英双语的专业术语作为标签保存，精准覆盖该图表的几何联动与视觉排版特征：

- **基础图形**: 桑基图 (Sankey diagram), 冲击图 (Alluvial diagram), 堆叠柱状图 (Stacked bar chart), 复合图表 (Composite chart/plot).
- **版式布局**: 左右并排布局 (Side-by-side layout), 跨图视觉绑定 (Cross-chart visual binding), 多坐标轴组合 (Multi-axes combination).
- **视觉细节**: 跨图表颜色同步映射 (Color synchronization/matching across charts), 自定义节点标签对齐 (Custom label alignment), 无边框柱体 (Edge-less bars), 半透明数据流带 (Translucent flow links).
- **适用场景**: 复杂资源/能量流向追踪与终端组成成分详细拆解联合分析。

### 📝 核心特征描述 (Prompt 提示词模板)

你可以直接复制这段描述作为该模板的 AI 提示词（Prompt）。这段话精准剥离了具体的分类数据，向 AI 提取了复刻该图表所需的拓扑结构和逻辑规则：

“这是一个**水平并排布局的复合型关联图表**。画布包含两个主要部分：**其一**是一个**多层桑基图 (Sankey Diagram)**，用于展示多级网络中数据流的分配与转移关系，节点和连带支持自定义透明度与标签位置；**其二**是一个**堆叠柱状图 (Stacked Bar Chart)**，用于详细展示某一特定维度或终端的分类数据构成。**该图表最核心的逻辑特征是色彩联动**：堆叠柱状图中各分段的颜色，必须通过变量名匹配，与桑基图中对应特定节点的颜色保持绝对一致，从而在视觉上实现跨图表的逻辑串联与数据呼应。”
