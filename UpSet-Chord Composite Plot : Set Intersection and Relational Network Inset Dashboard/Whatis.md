### 🎯 模板标准命名

**UpSet-弦图复合图 / 集合交集与关系网络画中画组合图 (UpSet-Chord Composite Plot / Set Intersection and Relational Network Inset Dashboard)**

### 🏷️ AI 检索高频关键词 (Tags)

建议将这些中英双语的高频专业术语作为标签保存，精准覆盖该图表的多维分析功能与嵌套排版特征：

- **基础图形** : UpSet图 (UpSet plot), 集合交集图 (Set intersection diagram), 弦图 (Chord diagram), 关系网络图 (Relationship network plot/chart).
- **布局与拓扑** : 画中画布局 (Inset axes / Picture-in-picture layout), 复合图表 (Composite chart), 多图表拼接 (Multi-axes combined panel).
- **视觉细节** : 矩阵点阵连线 (Matrix dot-line connections), 自定义排序 (Custom sorting degree/bit), 半透明连线 (Translucent ribbons), 翻转坐标轴 (Reversed axes).
- **适用场景** : 多维基因共有/特有分析 (Shared/unique gene analysis), 多物种互作与共现网络, 复杂归属与流向联合可视化.

### 📝 核心特征描述 (Prompt 提示词模板)

你可以直接复制这段描述作为该模板的 AI 提示词（Prompt）。这段话精准剥离了具体数据，向 AI 下达了复刻该图表所需的空间排版和底层绘图逻辑：

> “这是一个展示多维数据重叠与实体关系的**画中画复合图表 (Inset Composite Plot)**。图表的主体底层是一个 **UpSet 图**，由下方的集合成员关系点阵矩阵（带连接线）、上方表示交集大小的垂直条形图以及侧边表示集合总大小的水平条形图组成。在主体图表的上方或一侧留白处，通过自定义相对坐标系嵌套了一个 **双边/无向弦图 (Chord Diagram)**，其外围包含分类色块，内部通过半透明贝塞尔曲线展示节点间的映射与权重关系。整体布局紧凑，通过多坐标系拼接实现了集合定量分析与关系网络拓扑的视觉联动。”
